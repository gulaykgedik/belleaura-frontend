import Link from "next/link";
import type { Appointment } from "@/types/appointment";
import { StatusBadge } from "./status-badge";
import { appointmentDate, appointmentTime, money } from "./format";
export function AppointmentCard({appointment}:{appointment:Appointment}){return <article className="rounded-card border bg-card p-5 transition hover:border-primary/40"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-medium text-muted">{appointment.reference_no}</p><h2 className="mt-1 text-lg font-bold">{appointment.service_name_snapshot}</h2></div><StatusBadge status={appointment.status}/></div><dl className="mt-5 grid gap-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-muted">Personel</dt><dd className="font-medium">{appointment.staff_name}</dd></div><div className="flex justify-between gap-4"><dt className="text-muted">Tarih</dt><dd className="text-right font-medium">{appointmentDate(appointment.start_at)}</dd></div><div className="flex justify-between gap-4"><dt className="text-muted">Saat</dt><dd className="font-medium">{appointmentTime(appointment.start_at)}</dd></div><div className="flex justify-between gap-4"><dt className="text-muted">Fiyat</dt><dd className="font-semibold text-primary">{money(appointment.price_snapshot)}</dd></div></dl><Link href={`/appointments/${appointment.id}`} className="mt-5 inline-flex text-sm font-semibold text-primary hover:underline">Detayı görüntüle →</Link></article>;}
